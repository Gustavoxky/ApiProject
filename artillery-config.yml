config:
  target: 'http://localhost:4003'
  phases:
    - duration: 60
      arrivalRate: 300
  defaults:
    headers:
      Authorization: 'Bearer your-access-token'
scenarios:
  - flow:
      - get:
          url: "/users"
